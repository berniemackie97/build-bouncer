version: 1

checks:
  - name: "manual:configure"
    run: "echo TODO: configure build-bouncer checks 1>&2 && exit 1"

insults:
  mode: "snarky"
  file: "assets/insults/default.json"
  locale: "en"

banter:
  file: "assets/banter/default.json"
  locale: "en"
