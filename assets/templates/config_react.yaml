version: 1

checks:
  - name: "lint"
    run: "npm run lint"
  - name: "tests"
    run: "npm test"
  - name: "build"
    run: "npm run build"

insults:
  mode: "snarky"
  file: "assets/insults/default.json"
  locale: "en"

banter:
  file: "assets/banter/default.json"
  locale: "en"
