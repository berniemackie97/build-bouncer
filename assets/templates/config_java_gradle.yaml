version: 1

checks:
  - name: "tests"
    run: "./gradlew test"
  - name: "build"
    run: "./gradlew build"

insults:
  mode: "snarky"
  file: ".buildbouncer/assets/insults/default.json"
  locale: "en"

banter:
  file: ".buildbouncer/assets/banter/default.json"
  locale: "en"

