# Build Bouncer

A terminal bouncer for your repo: runs checks and blocks `git push` when things fail (with configurable insults).

## Quick start (later)
- Install git hook: `build-bouncer hook install`
- Configure checks in `.buildbouncer.yaml`
- Push and get roasted (politely, or not)

